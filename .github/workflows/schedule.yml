name: Daily TAO Parser

on:
  # запуск по расписанию в 00:00 UTC
  schedule:
    - cron: '0 0 * * *'
  # ручной запуск
  workflow_dispatch:

jobs:
  build:
    # если job идёт дольше 20 минут, Actions её сам прервет
    timeout-minutes: 20
    runs-on: ubuntu-latest

    strategy:
      matrix:
        # разбиваем диапазон NetUID на четыре части
        chunk: ["1-25", "26-50", "51-75", "76-100"]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install ChromeDriver
        run: |
          sudo apt-get update
          sudo apt-get install -y chromium-driver

      - name: Install Python dependencies
        run: |
          pip install -r requirements.txt bs4
          pip freeze

      - name: Write Google creds
        run: |
          printf '%s\n' "${{ secrets.GSPREAD_CREDS_JSON }}" > creds.json

      - name: Run parser (NetUID ${{ matrix.chunk }})
        env:
          GOOGLE_APPLICATION_CREDENTIALS: creds.json
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
          NETID_RANGE: ${{ matrix.chunk }}
        run: python taostats_autoparser.py
